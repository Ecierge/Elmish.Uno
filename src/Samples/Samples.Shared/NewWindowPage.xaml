<Page d:DataContext="{d:DesignInstance Type=samples:DesignData, IsDesignTimeCreatable=True}"
  x:Class="Elmish.Uno.Samples.NewWindow.NewWindowPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:samples="using:Elmish.Uno.Samples"
  xmlns:tconv="using:Microsoft.Toolkit.Uwp.UI.Converters"
  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
  mc:Ignorable="d">

  <Page.Resources>
    <tconv:BoolToVisibilityConverter x:Key="VisibilityConverter" />
  </Page.Resources>

  <Grid d:DataContext="{Binding NewWindow}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock HorizontalAlignment="Center" FontWeight="Bold">Page 1</TextBlock>

    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
      <Button Command="{Binding Window1Show}"  Content="Show"  Width="70" Margin="5" />
      <Button Command="{Binding Window1Close}" Content="Close" Width="70" Margin="5" />
    </StackPanel>
    <TextBlock
      Grid.Row="2"
      Margin="0,10,0,0"
      HorizontalAlignment="Center"
      FontWeight="Bold"
      Text="Page 2" />
    <Button
      Grid.Row="3"
      Width="70"
      Margin="0,5,0,5"
      HorizontalAlignment="Center"
      Command="{Binding Window2Show}"
      Content="Show" />
    <TextBlock Grid.Row="4">
      <Run Text="Window 1 input:" />
      <Run Text="{Binding Window1.Input}" />
    </TextBlock>
    <TextBlock Grid.Row="5">
      <Run Text="Window 2 input:" />
      <Run Text="{Binding Window2.Input}" />
    </TextBlock>
    <TextBlock Grid.Row="6">
      <Run Text="Window 2 checkbox:" />
      <Run Text="{Binding Window2.IsChecked}" />
    </TextBlock>
  </Grid>
</Page>
